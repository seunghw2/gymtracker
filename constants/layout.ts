/** 레이아웃 관련 매직넘버 모음(탭바·배너·숫자패드 높이 등). */

/** 커스텀 탭바 콘텐츠 높이(safe area 제외). */
export const TAB_BAR_CONTENT_HEIGHT = 59;

/** "운동 중" 배너 한 블록이 차지하는 세로 높이(바 + 여백). */
export const BANNER_BLOCK_HEIGHT = 60;

/** 인앱 숫자패드 추정 높이(라벨 포함). */
export const NUMPAD_HEIGHT = 264;

/** 숫자패드가 떠 있을 때 휴식 타이머를 올릴 높이(숫자패드 위 여유 포함). */
export const REST_TIMER_OVER_NUMPAD = 276;
